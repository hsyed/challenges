# TODO

* Fix the root flake. It currently pulls in Linux packages for the GPUI Conway's Game of Life project. Isolate that project so the root shell stays generic, possibly with a sub-flake or a project-specific dev shell.
